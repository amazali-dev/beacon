# Dashboard — copy to .env.local (local only, never commit)

VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_PUBLIC_KEY

# Local NON-US test banner (production data comes from GitHub Actions)
VITE_BEACON_ENV=staging
VITE_STAGING_LABEL=Local NON-US test
