# Dashboard — local only. Do not commit real keys.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_BEACON_ENV=staging
VITE_STAGING_LABEL=Local NON-US test
# Optional: fine-grained PAT (Actions write) so Run now triggers GitHub immediately.
# You can also paste the token once on the Operations page instead.
# VITE_GITHUB_DISPATCH_TOKEN=
# VITE_GITHUB_REPO=amazali-dev/beacon
